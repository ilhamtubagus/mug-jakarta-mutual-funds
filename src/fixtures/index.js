const accounts = require('./mockAccounts');
const investmentManagers = require('./mockInvestmentManagers');
const products = require('./mockProducts');
const navs = require('./mockNavs');
const portfolios = require('./portfolios');

module.exports = {
  accounts,
  investmentManagers,
  products,
  navs,
  portfolios,
};
